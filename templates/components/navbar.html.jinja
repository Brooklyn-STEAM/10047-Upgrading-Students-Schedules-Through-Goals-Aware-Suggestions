<nav class="bg-emerald-800 text-white">
  <div class="max-w-7xl mx-auto px-4">
    <div class="flex items-center h-16">

      <!-- Logo -->
      <a href="/" class="text-2xl font-bold tracking-wide transition-transform duration-200 hover:scale-105">
        CourseTrack
      </a>

      <!-- Right side (Desktop) -->
      <div class="hidden md:flex items-center ml-auto space-x-6">
        <a href="/dashboard" 
         class="relative py-1 transition text-white
         after:content-[''] after:absolute after:left-1/2 after:-translate-x-1/2
         after:bottom-0 after:h-[2px] after:w-0 after:bg-green-200
         after:transition-all after:duration-300
         hover:after:w-full">Dashboard</a>

        <a href=" /academicrecord" 
         class="relative py-1 transition text-white
         after:content-[''] after:absolute after:left-1/2 after:-translate-x-1/2
         after:bottom-0 after:h-[2px] after:w-0 after:bg-green-200
         after:transition-all after:duration-300
         hover:after:w-full">Academic Record</a>

        <a href="/recommendation" 
         class="relative py-1 transition text-white
         after:content-[''] after:absolute after:left-1/2 after:-translate-x-1/2
         after:bottom-0 after:h-[2px] after:w-0 after:bg-green-200
         after:transition-all after:duration-300
         hover:after:w-full">Recommendations</a>

        <a href="/preference" 
         class="relative py-1 transition text-white
         after:content-[''] after:absolute after:left-1/2 after:-translate-x-1/2
         after:bottom-0 after:h-[2px] after:w-0 after:bg-green-200
         after:transition-all after:duration-300
         hover:after:w-full">Preferences</a>
        

        <!-- Profile Dropdown -->
        <div class="relative">
          <button id="profileBtn" class="flex items-center focus:outline-none">
            <img src="/static/images/profile-icon.png" alt="Profile" class="w-9 h-9 rounded-full border-2 border-white">
          </button>

          <div id="profileMenu"
            class="hidden absolute right-0 mt-2 w-40 bg-white text-gray-700 rounded-md shadow-lg py-2">
            <a href="/profile" class="block px-4 py-2 hover:bg-gray-100">
              My Profile
            </a>
            <a href="/logout" class="block px-4 py-2 hover:bg-gray-100">
              Log Out
            </a>
          </div>
        </div>
      </div>

      <!-- Mobile menu button -->
      <div class="md:hidden ml-auto">
        <button id="mobileMenuBtn" class="focus:outline-none">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div id="mobileMenu" class="hidden md:hidden bg-emerald-700 px-4 pb-4 space-y-2">
    <a href="/" class="block hover:text-green-200">Home</a>
    <a href="/dashboard" class="block hover:text-green-200">Dashboard</a>
    <a href="/academicrecord" class="block hover:text-green-200">Academic Record</a>
    <a href="/recommendation" class="block hover:text-green-200">Recommendations</a>
    <a href="/preference" class="block hover:text-green-200">Preferences</a>

    <hr class="border-emerald-600">

    <a href="/profile" class="block hover:text-green-200">My Profile</a>
    <a href="/logout" class="block hover:text-green-200">Log Out</a>
  </div>
</nav>

<!-- Scripts -->
<script>
  const profileBtn = document.getElementById("profileBtn");
  const profileMenu = document.getElementById("profileMenu");
  const mobileMenuBtn = document.getElementById("mobileMenuBtn");
  const mobileMenu = document.getElementById("mobileMenu");

  profileBtn?.addEventListener("click", () => {
    profileMenu.classList.toggle("hidden");
  });

  mobileMenuBtn.addEventListener("click", () => {
    mobileMenu.classList.toggle("hidden");
  });

  window.addEventListener("click", (e) => {
    if (profileMenu && profileBtn &&
      !profileBtn.contains(e.target) &&
      !profileMenu.contains(e.target)) {
      profileMenu.classList.add("hidden");
    }
  });
</script>